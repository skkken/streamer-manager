# ===========================================
# Supabase
# ===========================================
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# ===========================================
# LINE Messaging API
# ===========================================
LINE_CHANNEL_ACCESS_TOKEN=your-line-channel-access-token
LINE_CHANNEL_SECRET=your-line-channel-secret

# ===========================================
# アプリケーション URL
# ===========================================
# 本番: https://your-domain.vercel.app
# 開発: http://localhost:3000
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ===========================================
# Vercel Cron ジョブ認証（現在未使用）
# ===========================================
# Cron を有効にする場合: openssl rand -hex 32 で生成し Vercel にも設定
# CRON_SECRET=your-cron-secret

# ===========================================
# Sentry エラー監視
# ===========================================
# https://sentry.io で取得:
#   1. プロジェクト作成 → Settings → Client Keys (DSN) から DSN を取得
#   2. Settings → Organization → General → Organization Slug を確認
#   3. Settings → Projects → Project Slug を確認
#   4. Settings → Auth Tokens → 新規トークン作成 (scope: project:releases, org:read)
NEXT_PUBLIC_SENTRY_DSN=https://examplePublicKey@o0.ingest.sentry.io/0
SENTRY_ORG=your-sentry-org
SENTRY_PROJECT=your-sentry-project
SENTRY_AUTH_TOKEN=your-sentry-auth-token
