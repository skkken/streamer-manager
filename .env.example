# ===========================================
# Supabase
# ===========================================
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# ===========================================
# LINE Messaging API
# ===========================================
# LINE チャネル認証情報は DB (line_channels テーブル) で管理
# 管理画面 /admin/line-channels から登録してください
# 以下の環境変数はフォールバック用（任意）
# LINE_CHANNEL_ACCESS_TOKEN=your-line-channel-access-token
# LINE_CHANNEL_SECRET=your-line-channel-secret

# ===========================================
# アプリケーション URL
# ===========================================
# 本番: https://your-domain.vercel.app
# 開発: http://localhost:3000
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ===========================================
# 暗号化キー (LINE チャネル認証情報の暗号化)
# ===========================================
# openssl rand -hex 32 で生成（64文字の16進数文字列 = 32バイト）
ENCRYPTION_KEY=your-encryption-key-hex-64-chars

# ===========================================
# Vercel Cron ジョブ認証
# ===========================================
# openssl rand -hex 32 で生成し Vercel 環境変数にも設定すること
CRON_SECRET=your-cron-secret

# ===========================================
# エラー監視 (Sentry)
# ===========================================
NEXT_PUBLIC_SENTRY_DSN=your-sentry-dsn
SENTRY_ORG=your-sentry-org
SENTRY_PROJECT=your-sentry-project
SENTRY_AUTH_TOKEN=your-sentry-auth-token
